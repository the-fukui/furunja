# ふるんじゃ（Web振り回しオタマトーン）v1 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** スマホを縦持ちのまま前後（X軸回転 = DeviceOrientation の beta）に傾けて音程を操作する、テルミン風のWeb楽器を1画面で作る。

**Architecture:** 純粋ロジック（傾き→半音→周波数）を `pitch.ts` に分離してユニットテストで固め、Web Audio の発音は `Voice` クラス（`audio.ts`）、センサー入力は薄いラッパー（`orientation.ts`）に分け、`main.ts` で配線する。UI は index.html + CSS のみで、フレームワークは使わない。

**Tech Stack:** Vite + TypeScript（vanilla、フレームワークなし）、Vitest、Web Audio API、DeviceOrientation API、@vitejs/plugin-basic-ssl（実機テスト用のLAN HTTPS）。

## Global Constraints

- 音程マッピング: 水平（beta = 0°）= A4 (440 Hz)。可動域は ±45° で、±12半音（合計2オクターブ、A3〜A5）に線形マッピング。範囲外はクランプ。
- 半音にスナップ（`Math.round`）し、音程間は `setTargetAtTime`（timeConstant 0.06 秒）のポルタメントで滑らかに繋ぐ。
- 発音は鳴りっぱなし方式。「ならす/とめる」トグルボタンのみ（机に置いたときのドローン対策として停止手段は必須）。
- 音色は1種のみ: sawtooth オシレーター → lowpass フィルタ（1100 Hz, Q=5）→ ゲイン。
- iOS 対応: `DeviceOrientationEvent.requestPermission()` と AudioContext 生成は必ずボタンタップ（ユーザー操作）起点で行う。
- 依存関係は devDependencies の `vite` / `typescript` / `vitest` / `@vitejs/plugin-basic-ssl` のみ。runtime 依存はゼロ。`latest` レンジは書かず、npm install が生成する具体的な semver レンジをそのまま使う。
- v1 に含めない（合意済み）: 録音、音色選択、スケール切替、赤ちゃんモード、キャリブレーション、マナーモード検出導線、顔アニメーション、PWA 化。
- コミットメッセージは日本語の `prefix: message` 形式。末尾に `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` を付ける。
- コードコメント・UI文言は日本語、識別子は英語。production code に `console.log` を残さない。

## ファイル構成

```
furunja/
├── index.html              # 1画面UI（タイトル、音名表示、トグルボタン）
├── package.json
├── tsconfig.json
├── vite.config.ts          # basic-ssl プラグイン（実機テスト用）
├── .gitignore
├── README.md               # Task 6 で作成
├── docs/superpowers/plans/ # この計画書
└── src/
    ├── main.ts             # UI配線（エントリポイント）
    ├── style.css           # 最小限のスタイル
    ├── pitch.ts            # 傾き→半音→周波数の純粋ロジック
    ├── pitch.test.ts
    ├── audio.ts            # Voice クラス（Web Audio 発音エンジン）
    ├── audio.test.ts
    ├── orientation.ts      # センサー許可＋deviceorientation リスナー
    └── orientation.test.ts
```

---

### Task 1: プロジェクト基盤（Vite + TypeScript + Vitest + git）

**Files:**
- Create: `.gitignore`
- Create: `package.json`（npm install で生成・更新）
- Create: `tsconfig.json`
- Create: `vite.config.ts`
- Create: `index.html`（仮。Task 5 で本実装に置き換える）
- Create: `src/main.ts`（仮。Task 5 で本実装に置き換える）

**Interfaces:**
- Consumes: なし（最初のタスク）
- Produces: `npm run dev` / `npm run build` / `npm test` が動く開発環境。後続タスクはすべてこの上で `vitest` を実行する。

- [ ] **Step 1: git リポジトリを初期化する**

```bash
cd /Users/k.fukui/codes/personal/furunja
git init
```

- [ ] **Step 2: .gitignore を作成する**

`.gitignore`:

```
node_modules/
dist/
*.local
.DS_Store
```

- [ ] **Step 3: package.json を作成し、開発依存をインストールする**

`package.json`（まず手書きで作成。バージョンは次の npm install が具体的なレンジで埋める）:

```json
{
  "name": "furunja",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest run"
  }
}
```

インストール（`latest` を手書きせず、npm に具体的な semver レンジを書かせる）:

```bash
npm install -D vite typescript vitest @vitejs/plugin-basic-ssl
```

Expected: `package.json` に devDependencies が `^x.y.z` 形式で追記される。

- [ ] **Step 4: tsconfig.json を作成する**

`tsconfig.json`:

```json
{
  "compilerOptions": {
    "target": "ES2022",
    "useDefineForClassFields": true,
    "module": "ESNext",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "skipLibCheck": true,
    "moduleResolution": "bundler",
    "noEmit": true,
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
```

- [ ] **Step 5: vite.config.ts を作成する**

basic-ssl は「iPhone実機からLAN経由でアクセスするとき、DeviceOrientation の許可APIが secure context を要求する」ための開発用。ビルド成果物には影響しない。

`vite.config.ts`:

```ts
import { defineConfig } from 'vite';
import basicSsl from '@vitejs/plugin-basic-ssl';

export default defineConfig({
  plugins: [basicSsl()],
  server: {
    host: true,
  },
});
```

- [ ] **Step 6: 仮の index.html と src/main.ts を作成する**

`index.html`（Task 5 で置き換える仮実装）:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>ふるんじゃ</title>
  </head>
  <body>
    <p>準備中</p>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

`src/main.ts`（仮実装）:

```ts
export {};
```

- [ ] **Step 7: dev サーバーとビルドが動くことを確認する**

```bash
npm run build
```

Expected: `dist/` が生成され、エラーなく完了する。

```bash
npm run dev
```

Expected: `https://localhost:5173/`（basic-ssl により https）が起動する。ブラウザで開いて「準備中」が表示されることを確認したら Ctrl+C で停止。

- [ ] **Step 8: コミット**

```bash
git add .gitignore package.json package-lock.json tsconfig.json vite.config.ts index.html src/main.ts docs/
git commit -m "chore: Vite + TypeScript + Vitest のプロジェクト基盤を作成"
```

---

### Task 2: ピッチマッピング（傾き→半音→周波数の純粋ロジック）

**Files:**
- Create: `src/pitch.ts`
- Test: `src/pitch.test.ts`

**Interfaces:**
- Consumes: なし（純粋関数のみ）
- Produces:
  - `MAX_TILT_DEG: number`（45）
  - `SEMITONE_RANGE: number`（12）
  - `CENTER_FREQ_HZ: number`（440）
  - `clampTilt(betaDeg: number): number`
  - `tiltToSemitone(betaDeg: number): number` — クランプ＋線形マッピング＋半音スナップ済みの整数を返す
  - `semitoneToFrequency(semitone: number): number` — A4基準の平均律周波数(Hz)
  - `semitoneToNoteName(semitone: number): string` — 表示用音名（例: `"A4"`, `"C5"`）

- [ ] **Step 1: 失敗するテストを書く**

`src/pitch.test.ts`:

```ts
import { describe, it, expect } from 'vitest';
import {
  clampTilt,
  tiltToSemitone,
  semitoneToFrequency,
  semitoneToNoteName,
} from './pitch';

describe('clampTilt', () => {
  it('範囲内の値はそのまま返す', () => {
    expect(clampTilt(30)).toBe(30);
    expect(clampTilt(-30)).toBe(-30);
  });

  it('+45度を超える値は+45にクランプする', () => {
    expect(clampTilt(80)).toBe(45);
  });

  it('-45度未満の値は-45にクランプする', () => {
    expect(clampTilt(-90)).toBe(-45);
  });
});

describe('tiltToSemitone', () => {
  it('水平(0度)は中央の0半音', () => {
    expect(tiltToSemitone(0)).toBe(0);
  });

  it('+45度は+12半音(1オクターブ上)', () => {
    expect(tiltToSemitone(45)).toBe(12);
  });

  it('-45度は-12半音(1オクターブ下)', () => {
    expect(tiltToSemitone(-45)).toBe(-12);
  });

  it('中間の傾きは最も近い半音にスナップする', () => {
    // 5度 → 5/45*12 = 1.33半音 → 1半音
    expect(tiltToSemitone(5)).toBe(1);
  });

  it('可動域の外はクランプされる', () => {
    expect(tiltToSemitone(90)).toBe(12);
    expect(tiltToSemitone(-180)).toBe(-12);
  });
});

describe('semitoneToFrequency', () => {
  it('0半音は440Hz(A4)', () => {
    expect(semitoneToFrequency(0)).toBeCloseTo(440);
  });

  it('+12半音は880Hz(A5)', () => {
    expect(semitoneToFrequency(12)).toBeCloseTo(880);
  });

  it('-12半音は220Hz(A3)', () => {
    expect(semitoneToFrequency(-12)).toBeCloseTo(220);
  });

  it('+1半音は約466.16Hz(A#4)', () => {
    expect(semitoneToFrequency(1)).toBeCloseTo(466.16, 1);
  });
});

describe('semitoneToNoteName', () => {
  it('0半音はA4', () => {
    expect(semitoneToNoteName(0)).toBe('A4');
  });

  it('+12半音はA5', () => {
    expect(semitoneToNoteName(12)).toBe('A5');
  });

  it('-12半音はA3', () => {
    expect(semitoneToNoteName(-12)).toBe('A3');
  });

  it('+3半音はC5(オクターブ境界を正しく跨ぐ)', () => {
    expect(semitoneToNoteName(3)).toBe('C5');
  });

  it('-10半音はB3', () => {
    expect(semitoneToNoteName(-10)).toBe('B3');
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
npx vitest run src/pitch.test.ts
```

Expected: FAIL。`Failed to load ... ./pitch`（モジュール未作成）で全テストが落ちる。

- [ ] **Step 3: 実装を書く**

`src/pitch.ts`:

```ts
// 傾き（DeviceOrientation の beta、単位: 度）から音程への純粋な変換ロジック。
// 水平(0度)を中央 A4 とし、±45度を ±12半音（合計2オクターブ）へ線形マッピングする。

export const MAX_TILT_DEG = 45;
export const SEMITONE_RANGE = 12;
export const CENTER_FREQ_HZ = 440; // A4

// MIDIノート番号でのA4。音名変換に使う。
const MIDI_A4 = 69;
const NOTE_NAMES = ['C', 'C#', 'D', 'D#', 'E', 'F', 'F#', 'G', 'G#', 'A', 'A#', 'B'];
const SEMITONES_PER_OCTAVE = 12;

export function clampTilt(betaDeg: number): number {
  return Math.min(MAX_TILT_DEG, Math.max(-MAX_TILT_DEG, betaDeg));
}

export function tiltToSemitone(betaDeg: number): number {
  return Math.round((clampTilt(betaDeg) / MAX_TILT_DEG) * SEMITONE_RANGE);
}

export function semitoneToFrequency(semitone: number): number {
  return CENTER_FREQ_HZ * Math.pow(2, semitone / SEMITONES_PER_OCTAVE);
}

export function semitoneToNoteName(semitone: number): string {
  const midi = MIDI_A4 + semitone;
  const name = NOTE_NAMES[((midi % SEMITONES_PER_OCTAVE) + SEMITONES_PER_OCTAVE) % SEMITONES_PER_OCTAVE];
  const octave = Math.floor(midi / SEMITONES_PER_OCTAVE) - 1;
  return `${name}${octave}`;
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
npx vitest run src/pitch.test.ts
```

Expected: PASS（17 tests）。

- [ ] **Step 5: コミット**

```bash
git add src/pitch.ts src/pitch.test.ts
git commit -m "feat: 傾き→半音→周波数のピッチマッピングを実装"
```

---

### Task 3: 発音エンジン（Voice クラス）

**Files:**
- Create: `src/audio.ts`
- Test: `src/audio.test.ts`

**Interfaces:**
- Consumes: なし（Web Audio API のみ。テストでは fake AudioContext を注入）
- Produces:
  - `MASTER_GAIN: number`（0.4）
  - `PORTAMENTO_TIME_CONSTANT: number`（0.06）
  - `GAIN_RAMP_TIME_CONSTANT: number`（0.02）
  - `class Voice`:
    - `constructor(ctx: AudioContext)` — 生成時にオーディオグラフを組み、オシレーターを起動する（ゲイン0なので無音）
    - `start(): void` — ゲインを上げて発音開始
    - `stop(): void` — ゲインを0に落として無音化（オシレーターは止めない）
    - `setFrequency(frequencyHz: number): void` — ポルタメント付きで周波数変更
    - `get isPlaying(): boolean`

- [ ] **Step 1: 失敗するテストを書く**

Web Audio はブラウザAPIなので、記録用の fake を使って「正しいグラフを組むこと」「AudioParam を正しく操作すること」を検証する。

`src/audio.test.ts`:

```ts
import { describe, it, expect, beforeEach } from 'vitest';
import {
  Voice,
  MASTER_GAIN,
  PORTAMENTO_TIME_CONSTANT,
  GAIN_RAMP_TIME_CONSTANT,
} from './audio';

class FakeAudioParam {
  value = 0;
  calls: Array<{ target: number; startTime: number; timeConstant: number }> = [];

  setTargetAtTime(target: number, startTime: number, timeConstant: number): void {
    this.calls.push({ target, startTime, timeConstant });
  }

  lastCall() {
    return this.calls[this.calls.length - 1];
  }
}

class FakeNode {
  connectedTo: unknown[] = [];

  connect(node: unknown): unknown {
    this.connectedTo.push(node);
    return node;
  }
}

class FakeOscillator extends FakeNode {
  type = 'sine';
  frequency = new FakeAudioParam();
  started = false;

  start(): void {
    this.started = true;
  }
}

class FakeBiquadFilter extends FakeNode {
  type = 'lowpass';
  frequency = new FakeAudioParam();
  Q = new FakeAudioParam();
}

class FakeGain extends FakeNode {
  gain = new FakeAudioParam();
}

class FakeAudioContext {
  currentTime = 1.5;
  destination = new FakeNode();
  oscillator = new FakeOscillator();
  filter = new FakeBiquadFilter();
  gainNode = new FakeGain();

  createOscillator() {
    return this.oscillator;
  }

  createBiquadFilter() {
    return this.filter;
  }

  createGain() {
    return this.gainNode;
  }
}

describe('Voice', () => {
  let fakeCtx: FakeAudioContext;
  let voice: Voice;

  beforeEach(() => {
    fakeCtx = new FakeAudioContext();
    voice = new Voice(fakeCtx as unknown as AudioContext);
  });

  it('オシレーター→フィルタ→ゲイン→出力のグラフを組む', () => {
    expect(fakeCtx.oscillator.connectedTo).toContain(fakeCtx.filter);
    expect(fakeCtx.filter.connectedTo).toContain(fakeCtx.gainNode);
    expect(fakeCtx.gainNode.connectedTo).toContain(fakeCtx.destination);
  });

  it('音色はsawtooth + lowpassフィルタ', () => {
    expect(fakeCtx.oscillator.type).toBe('sawtooth');
    expect(fakeCtx.filter.type).toBe('lowpass');
  });

  it('生成直後はゲイン0(無音)でオシレーターは起動済み', () => {
    expect(fakeCtx.gainNode.gain.value).toBe(0);
    expect(fakeCtx.oscillator.started).toBe(true);
    expect(voice.isPlaying).toBe(false);
  });

  it('start()でゲインをMASTER_GAINへ滑らかに上げる', () => {
    voice.start();
    expect(fakeCtx.gainNode.gain.lastCall()).toEqual({
      target: MASTER_GAIN,
      startTime: fakeCtx.currentTime,
      timeConstant: GAIN_RAMP_TIME_CONSTANT,
    });
    expect(voice.isPlaying).toBe(true);
  });

  it('stop()でゲインを0へ滑らかに落とす', () => {
    voice.start();
    voice.stop();
    expect(fakeCtx.gainNode.gain.lastCall()).toEqual({
      target: 0,
      startTime: fakeCtx.currentTime,
      timeConstant: GAIN_RAMP_TIME_CONSTANT,
    });
    expect(voice.isPlaying).toBe(false);
  });

  it('setFrequency()はポルタメント付きで周波数を変更する', () => {
    voice.setFrequency(660);
    expect(fakeCtx.oscillator.frequency.lastCall()).toEqual({
      target: 660,
      startTime: fakeCtx.currentTime,
      timeConstant: PORTAMENTO_TIME_CONSTANT,
    });
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
npx vitest run src/audio.test.ts
```

Expected: FAIL。`Failed to load ... ./audio` で落ちる。

- [ ] **Step 3: 実装を書く**

`src/audio.ts`:

```ts
// Web Audio による発音エンジン。
// sawtooth + lowpass で「にゃー」寄りの音色を1種だけ持つ。
// 鳴りっぱなし方式のため、start/stop はオシレーターではなくゲインで制御する
// （オシレーターの stop は不可逆で、都度再生成するとクリックノイズが出やすいため）。

export const MASTER_GAIN = 0.4;
export const PORTAMENTO_TIME_CONSTANT = 0.06; // 半音間を滑らかに繋ぐポルタメント
export const GAIN_RAMP_TIME_CONSTANT = 0.02; // 発音・停止時のクリックノイズ防止

const FILTER_FREQUENCY_HZ = 1100;
const FILTER_Q = 5;
const INITIAL_FREQUENCY_HZ = 440;

export class Voice {
  private readonly ctx: AudioContext;
  private readonly oscillator: OscillatorNode;
  private readonly gain: GainNode;
  private playing = false;

  constructor(ctx: AudioContext) {
    this.ctx = ctx;

    this.oscillator = ctx.createOscillator();
    this.oscillator.type = 'sawtooth';
    this.oscillator.frequency.value = INITIAL_FREQUENCY_HZ;

    const filter = ctx.createBiquadFilter();
    filter.type = 'lowpass';
    filter.frequency.value = FILTER_FREQUENCY_HZ;
    filter.Q.value = FILTER_Q;

    this.gain = ctx.createGain();
    this.gain.gain.value = 0;

    this.oscillator.connect(filter);
    filter.connect(this.gain);
    this.gain.connect(ctx.destination);
    this.oscillator.start();
  }

  get isPlaying(): boolean {
    return this.playing;
  }

  start(): void {
    this.gain.gain.setTargetAtTime(MASTER_GAIN, this.ctx.currentTime, GAIN_RAMP_TIME_CONSTANT);
    this.playing = true;
  }

  stop(): void {
    this.gain.gain.setTargetAtTime(0, this.ctx.currentTime, GAIN_RAMP_TIME_CONSTANT);
    this.playing = false;
  }

  setFrequency(frequencyHz: number): void {
    this.oscillator.frequency.setTargetAtTime(
      frequencyHz,
      this.ctx.currentTime,
      PORTAMENTO_TIME_CONSTANT,
    );
  }
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
npx vitest run src/audio.test.ts
```

Expected: PASS（6 tests）。

- [ ] **Step 5: コミット**

```bash
git add src/audio.ts src/audio.test.ts
git commit -m "feat: Web Audio 発音エンジン Voice を実装"
```

---

### Task 4: センサー入力（許可リクエスト＋傾きリスナー）

**Files:**
- Create: `src/orientation.ts`
- Test: `src/orientation.test.ts`

**Interfaces:**
- Consumes: なし（ブラウザの `DeviceOrientationEvent` / `window`。テストでは `vi.stubGlobal` で差し替え）
- Produces:
  - `requestOrientationPermission(): Promise<boolean>` — iOS では `DeviceOrientationEvent.requestPermission()` を呼び、granted なら true。API が存在しない環境（Android・PC）では常に true
  - `listenTilt(onTilt: (betaDeg: number) => void): () => void` — `deviceorientation` イベントの beta をコールバックへ渡す。戻り値は解除関数

- [ ] **Step 1: 失敗するテストを書く**

`src/orientation.test.ts`:

```ts
import { describe, it, expect, vi, afterEach } from 'vitest';
import { requestOrientationPermission, listenTilt } from './orientation';

afterEach(() => {
  vi.unstubAllGlobals();
});

describe('requestOrientationPermission', () => {
  it('requestPermissionが無い環境(Android/PC)ではtrueを返す', async () => {
    vi.stubGlobal('DeviceOrientationEvent', class {});
    await expect(requestOrientationPermission()).resolves.toBe(true);
  });

  it('grantedならtrueを返す', async () => {
    class WithPermission {
      static requestPermission = vi.fn().mockResolvedValue('granted');
    }
    vi.stubGlobal('DeviceOrientationEvent', WithPermission);
    await expect(requestOrientationPermission()).resolves.toBe(true);
  });

  it('deniedならfalseを返す', async () => {
    class WithPermission {
      static requestPermission = vi.fn().mockResolvedValue('denied');
    }
    vi.stubGlobal('DeviceOrientationEvent', WithPermission);
    await expect(requestOrientationPermission()).resolves.toBe(false);
  });
});

describe('listenTilt', () => {
  function stubWindow() {
    const listeners: Record<string, (event: unknown) => void> = {};
    const removed: string[] = [];
    vi.stubGlobal('window', {
      addEventListener: (type: string, fn: (event: unknown) => void) => {
        listeners[type] = fn;
      },
      removeEventListener: (type: string) => {
        removed.push(type);
      },
    });
    return { listeners, removed };
  }

  it('deviceorientationイベントのbetaをコールバックへ渡す', () => {
    const { listeners } = stubWindow();
    const received: number[] = [];

    listenTilt((betaDeg) => received.push(betaDeg));
    listeners['deviceorientation']({ beta: 30 });
    listeners['deviceorientation']({ beta: -12.5 });

    expect(received).toEqual([30, -12.5]);
  });

  it('betaがnullのイベントは無視する', () => {
    const { listeners } = stubWindow();
    const received: number[] = [];

    listenTilt((betaDeg) => received.push(betaDeg));
    listeners['deviceorientation']({ beta: null });

    expect(received).toEqual([]);
  });

  it('返された関数でリスナーを解除できる', () => {
    const { removed } = stubWindow();

    const unlisten = listenTilt(() => {});
    unlisten();

    expect(removed).toEqual(['deviceorientation']);
  });
});
```

- [ ] **Step 2: テストが失敗することを確認する**

```bash
npx vitest run src/orientation.test.ts
```

Expected: FAIL。`Failed to load ... ./orientation` で落ちる。

- [ ] **Step 3: 実装を書く**

`src/orientation.ts`:

```ts
// DeviceOrientation まわりの薄いラッパー。
// iOS Safari だけが requestPermission を要求するため、存在チェックで分岐する。

type PermissionRequester = {
  requestPermission?: () => Promise<'granted' | 'denied'>;
};

export async function requestOrientationPermission(): Promise<boolean> {
  const eventClass = DeviceOrientationEvent as unknown as PermissionRequester;
  if (typeof eventClass.requestPermission !== 'function') {
    return true;
  }
  return (await eventClass.requestPermission()) === 'granted';
}

export function listenTilt(onTilt: (betaDeg: number) => void): () => void {
  const handler = (event: DeviceOrientationEvent) => {
    if (event.beta === null) {
      return;
    }
    onTilt(event.beta);
  };
  window.addEventListener('deviceorientation', handler);
  return () => window.removeEventListener('deviceorientation', handler);
}
```

- [ ] **Step 4: テストが通ることを確認する**

```bash
npx vitest run src/orientation.test.ts
```

Expected: PASS（6 tests）。

- [ ] **Step 5: 全テストを回してリグレッションがないことを確認する**

```bash
npm test
```

Expected: PASS（pitch 17 + audio 6 + orientation 6 = 29 tests）。

- [ ] **Step 6: コミット**

```bash
git add src/orientation.ts src/orientation.test.ts
git commit -m "feat: DeviceOrientation の許可リクエストと傾きリスナーを実装"
```

---

### Task 5: UI 組み立てと配線（index.html / style.css / main.ts）

**Files:**
- Modify: `index.html`（Task 1 の仮実装を全置換）
- Modify: `src/main.ts`（Task 1 の仮実装を全置換）
- Create: `src/style.css`

**Interfaces:**
- Consumes:
  - `tiltToSemitone(betaDeg: number): number` / `semitoneToFrequency(semitone: number): number` / `semitoneToNoteName(semitone: number): string`（Task 2）
  - `class Voice`: `constructor(ctx: AudioContext)` / `start()` / `stop()` / `setFrequency(frequencyHz: number)` / `isPlaying`（Task 3）
  - `requestOrientationPermission(): Promise<boolean>` / `listenTilt(onTilt: (betaDeg: number) => void): () => void`（Task 4）
- Produces: 完成した1画面アプリ（後続タスクは README のみ）

- [ ] **Step 1: index.html を本実装に置き換える**

`index.html`（全置換）:

```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0, user-scalable=no" />
    <title>ふるんじゃ — かたむけて鳴らすWebオタマトーン</title>
  </head>
  <body>
    <main>
      <h1>ふるんじゃ</h1>
      <p id="note" aria-live="polite">&mdash;</p>
      <button id="toggle" type="button">ならす</button>
      <p id="status" aria-live="polite"></p>
    </main>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: src/style.css を作成する**

`src/style.css`:

```css
:root {
  color-scheme: light dark;
}

* {
  box-sizing: border-box;
}

body {
  margin: 0;
  font-family: system-ui, sans-serif;
  min-height: 100dvh;
  display: grid;
  place-items: center;
}

main {
  text-align: center;
  padding: 24px;
}

h1 {
  font-size: 1.4rem;
  margin: 0;
}

#note {
  font-size: 4rem;
  font-weight: 700;
  margin: 24px 0;
  min-height: 1.2em;
  font-variant-numeric: tabular-nums;
}

#toggle {
  font-size: 1.5rem;
  padding: 16px 48px;
  border-radius: 999px;
  border: none;
  background: #f08300;
  color: #fff;
  cursor: pointer;
  touch-action: manipulation;
}

#status {
  min-height: 1.5em;
  opacity: 0.7;
}
```

- [ ] **Step 3: src/main.ts を本実装に置き換える**

`src/main.ts`（全置換）:

```ts
import './style.css';
import { tiltToSemitone, semitoneToFrequency, semitoneToNoteName } from './pitch';
import { Voice } from './audio';
import { requestOrientationPermission, listenTilt } from './orientation';

function mustQuery<T extends Element>(selector: string): T {
  const element = document.querySelector<T>(selector);
  if (!element) {
    throw new Error(`必須のUI要素が見つかりません: ${selector}`);
  }
  return element;
}

const toggleButton = mustQuery<HTMLButtonElement>('#toggle');
const noteDisplay = mustQuery<HTMLParagraphElement>('#note');
const statusDisplay = mustQuery<HTMLParagraphElement>('#status');

let voice: Voice | null = null;
let currentSemitone: number | null = null;

function handleTilt(betaDeg: number): void {
  if (!voice?.isPlaying) {
    return;
  }
  const semitone = tiltToSemitone(betaDeg);
  if (semitone === currentSemitone) {
    return;
  }
  currentSemitone = semitone;
  voice.setFrequency(semitoneToFrequency(semitone));
  noteDisplay.textContent = semitoneToNoteName(semitone);
}

async function startPlaying(): Promise<void> {
  // iOSではセンサー許可もAudioContext生成もユーザー操作起点が必須のため、
  // どちらもこのクリックハンドラ内で行う。
  const granted = await requestOrientationPermission();
  if (!granted) {
    statusDisplay.textContent = 'センサーの利用が許可されませんでした。ページを再読み込みしてやり直せます。';
    return;
  }

  if (!voice) {
    const ctx = new AudioContext();
    await ctx.resume();
    voice = new Voice(ctx);
    listenTilt(handleTilt);
  }

  voice.start();
  toggleButton.textContent = 'とめる';
  statusDisplay.textContent = '本体を前後にかたむけて音程をかえよう';
}

function stopPlaying(): void {
  voice?.stop();
  toggleButton.textContent = 'ならす';
  statusDisplay.textContent = '';
}

toggleButton.addEventListener('click', () => {
  if (voice?.isPlaying) {
    stopPlaying();
  } else {
    void startPlaying();
  }
});
```

- [ ] **Step 4: 型チェックとビルドが通ることを確認する**

```bash
npm run build
```

Expected: エラーなく `dist/` が生成される。

- [ ] **Step 5: 全テストを回す**

```bash
npm test
```

Expected: PASS（29 tests、Task 4 時点から変化なし）。

- [ ] **Step 6: PCブラウザで手動検証する（センサーエミュレーション）**

```bash
npm run dev
```

Chrome で `https://localhost:5173/` を開き（自己署名証明書の警告は「詳細設定→続行」で通過）:

1. DevTools → その他のツール → センサー（Sensors）パネルを開く
2. Orientation を「Custom orientation」にして beta を操作する
3. 「ならす」をタップ → 音が鳴り始める
4. beta = 0 → 表示が「A4」、beta = 45 → 「A5」、beta = -45 → 「A3」
5. beta をゆっくり動かす → 半音ごとに階段状に変わりつつ、音は滑らかに繋がる
6. beta = 90 など範囲外 → A5 のままクランプされる
7. 「とめる」→ 無音になり、「ならす」で再開できる

- [ ] **Step 7: iPhone 実機で手動検証する**

1. `npm run dev` を起動したまま、Mac と iPhone を同じ Wi-Fi に接続する
2. dev サーバー起動ログに表示される `https://<LAN-IP>:5173/` を iPhone Safari で開く（自己署名証明書の警告を通過する）
3. 「ならす」をタップ → センサー許可ダイアログ → 「許可」
4. マナーモード（サイレントスイッチ）をオフにして音が鳴ることを確認する
5. 縦持ちで水平（画面上向き）→ A4。手前に45度起こす → 音程が上がって A5。逆に45度傾ける → A3
6. ゆっくり傾けて、半音スナップ＋ポルタメントの「にゃ〜」という繋がりを確認する
7. 机に置く → A4 が鳴り続ける（仕様どおり）。「とめる」で止まることを確認する

Expected: 上記すべてが動作する。音色・可動域の体感に問題があれば `FILTER_FREQUENCY_HZ` / `FILTER_Q` / `PORTAMENTO_TIME_CONSTANT` の定数調整で対応する（構造変更はしない）。

- [ ] **Step 8: コミット**

```bash
git add index.html src/main.ts src/style.css
git commit -m "feat: 演奏画面のUIを実装しピッチ・音源・センサーを配線"
```

---

### Task 6: README と本番ビルドの最終確認

**Files:**
- Create: `README.md`

**Interfaces:**
- Consumes: 完成したアプリ（Task 5）
- Produces: なし（最終タスク）

- [ ] **Step 1: README.md を作成する**

`README.md`:

```markdown
# ふるんじゃ

スマホを縦持ちのまま前後にかたむけて音程を操作する、テルミン風のWebオタマトーン。

## あそびかた

1. スマホでページを開き、「ならす」をタップする
2. センサーの利用を許可する（iOSのみダイアログが出る）
3. 本体を水平にすると真ん中の音（A4）、手前に起こすと高く、逆にかたむけると低くなる
4. 可動域は水平から±45度で、2オクターブ（A3〜A5）
5. 音は半音にスナップしつつ、間はポルタメントで滑らかにつながる
6. 「とめる」で音を止める

## 開発

```bash
npm install
npm run dev    # https://localhost:5173 (実機テスト用にLAN公開+自己署名HTTPS)
npm test       # ユニットテスト (Vitest)
npm run build  # 本番ビルド → dist/
```

iPhone実機で試すときは、同じWi-FiにつないでdevサーバーのLAN URLをSafariで開く。
DeviceOrientationの許可APIがsecure contextを要求するため、HTTPSが必須
（開発時は @vitejs/plugin-basic-ssl の自己署名証明書を使う）。

## デプロイ

`npm run build` の `dist/` を任意の静的ホスティング（Vercel、GitHub Pages 等）へ配置する。
HTTPS配信が必須。

## 既知の制約（v1の仕様）

- 鳴りっぱなし方式のため、机に置くと中央の音が鳴り続ける（「とめる」で停止）
- iOSのマナーモード（サイレントスイッチ）がオンだと音が出ない場合がある。検出・案内はしない
- キャリブレーションなし。±45度の固定マッピング
- 録音、音色選択、スケール切替、赤ちゃんモード、PWA化は含まない
```

- [ ] **Step 2: 本番ビルドをプレビューで最終確認する**

```bash
npm run build
npm run preview
```

Expected: ビルド成功。表示された URL をブラウザで開き、Task 5 Step 6 の 3〜7 と同じ動作を確認する（preview は http のため、センサーエミュレーションでの確認で可）。

- [ ] **Step 3: 全テストの最終確認**

```bash
npm test
```

Expected: PASS（29 tests）。

- [ ] **Step 4: コミット**

```bash
git add README.md
git commit -m "docs: README に遊び方・開発手順・既知の制約を記載"
```
